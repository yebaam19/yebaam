/** Re-export: the Hero was moved to ./hero/HeroSection.tsx and split into
 *  focused sub-components. This file keeps the original import path working. */
export { HeroSection } from './hero/HeroSection'
