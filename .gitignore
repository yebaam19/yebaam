# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.key
*.crt
*.p12
*.pfx
*.pdf

# debug
yarn-debug.log*
yarn-error.log*
*.log
logs/

# env files — never commit any .env variant
.env
.env.*
.env.local
.env.development
.env.staging
.env.production
.env.test
!.env.example

# prisma generated client
/src/lib/prisma/generated

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# editor / IDE
.cursor
.vscode/
.idea/
*.swp
*.swo

# AWS credentials & config
.aws/
*.csv
aws-exports.js
amplify-meta.json
amplify/.config/local-*

# secrets & keys
secrets/
*.secret
*.secrets
*_credentials*
*_accessKeys*
serviceAccountKey.json
*firebase*.json
*firebase-adminsdk*
google-credentials.json
gcloud-credentials.json

# terraform (no longer used but keep ignored)
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/
.terraform.lock.hcl
tfplan*

# docker
.docker/
docker-compose.override.yml

# CI/CD
.github/workflows/

# misc build artifacts
.git-rewrite/
dist/
.cache/

# SQL files — never commit (migrations, dumps, scratch queries)
*.sql

# MCP config (contains API keys)
.mcp.json
.mcp.*.json
mcp.json
mcp-config.json
**/.mcp.json
**/mcp.json

# API keys, tokens & generic secrets
*api*key*
*api-key*
*apikey*
*access*token*
*auth*token*
*bearer*token*
*private*key*
*.key.json
*.token
*.jwt
.htpasswd

# additional env / config leaks
.env.backup
.env.bak
.env.old
.env.save
.envrc
*.env
config.local.*
local.settings.json

# Supabase local config & keys
.supabase/
supabase/.branches/
supabase/.temp/
supabase/config.local.toml

# misc credentials
credentials
credentials.json
client_secret*.json
token.json
tokens.json
# Stray OAuth/client dump at repo root — do NOT glob `auth.json` (blocks messages/*/auth.json translations).
/auth.json

# markdown docs — don't push to repo
*.md
!README.md
# YEBAAM governance source-of-truth — keep tracked despite the *.md ignore above
!docs/legal/
!docs/legal/**

# InsForge & AI agent skills
.insforge
.agent
.agents
.augment
# .claude is ignored EXCEPT the governance subagents (.claude/agents/), which are tracked.
# Use .claude/* (ignore children) so the agents/ negation below can take effect — a bare
# `.claude` would exclude the parent dir and make the negation a no-op.
.claude/*
!.claude/agents/
!.claude/agents/**
.cline
.github/copilot*
.kilocode
.qoder
.qwen
.roo
.trae
.windsurf
